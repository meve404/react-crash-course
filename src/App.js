function App() {
  
  return (
    <div className="container">
      <h1>Hello from react</h1>
      {/* <h2>{x ? name : 'No'}</h2> x ? : == if x then ... */}
      
    </div>
  );
}

export default App;
