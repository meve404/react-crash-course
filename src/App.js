import { Header } from "./components/Header";

function App() {

  return (
    <div className="container">
      {/* <h2>{x ? name : 'No'}</h2> x ? : == if x then ... */}
      <Header></Header>
    </div>
  );
}

export default App;
